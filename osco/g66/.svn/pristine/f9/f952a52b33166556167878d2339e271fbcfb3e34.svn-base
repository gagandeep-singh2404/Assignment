#ifndef QUEE
#define QUEE

#include <stdlib.h>
#define SizeOfArray 3

typedef struct structPage{
    int pageNr;
    int age;
} Page;


void incrementAge(Page*);
void changePageNr(int* oldPage, int newPage);
void createArray(Page *array);
void printArray(Page *array);
void doFifo(Page *array, int[], int);
void doLRU(Page *array, int[], int);
void c(Page *array, int[]);
#endif
